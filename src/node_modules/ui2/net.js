var net = require('net');
var optionsmod = require('./optionsbase');

// 继承与net，支持setOption接口，这样就可以支持option的传递设置了
exports.Request = new Class(net.Request, function() {
	this.setOption = optionsmod.overloadsetter(function(self, name, value) {
		self[name] = value;
	});
});
