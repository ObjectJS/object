module("object-function");

test('extend', function() {
	ok(true, 'object');
});

test('clone', function() {
	ok(true, 'object');
});

test('bind', function() {
	ok(true, 'object');
});

test('use', function() {
	ok(true, 'object');
});

test('add', function() {
	ok(true, 'object');
});

test('execute', function() {
	ok(true, 'object');
});
