module("loader-function");

test('loadScript', function() {
	ok(true, 'loader');
});

test('getUses', function() {
	ok(true, 'loader');
});

test('makePrefixPackage', function() {
	ok(true, 'loader');
});

test('loadLib', function() {
	ok(true, 'loader');
});

test('add', function() {
	ok(true, 'loader');
});

test('use', function() {
	ok(true, 'loader');
});

test('execute', function() {
	ok(true, 'loader');
});
